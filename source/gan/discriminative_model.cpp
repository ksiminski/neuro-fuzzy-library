
#include "discriminative_model.h"

ksi::discriminative_model::discriminative_model ()
{
}

ksi::discriminative_model::discriminative_model (const ksi::discriminative_model & wzor)
{
}

ksi::discriminative_model::discriminative_model (ksi::discriminative_model && wzor)
{
}

ksi::discriminative_model & ksi::discriminative_model::operator= (const ksi::discriminative_model & wzor)
{
   if (this == & wzor)
      return *this;

   // remove what is to remove

   // allocate what is to allocate

   return *this;
}

ksi::discriminative_model & ksi::discriminative_model::operator= (ksi::discriminative_model && wzor)
{
   if (this == & wzor)
      return *this;

   // swap what is to swap

   return *this;
}

ksi::discriminative_model::~discriminative_model ()
{

   // delete what is to delete

}

 

